---
"codemirror-lang-rics": minor
---

Add color highlighter extension for inline color preview in editor. Includes `colorHighlighter()` function and `colorHighlighterStyles` theme. Supports hex, rgb, hsl, hwb, lab, lch, oklch, oklab, and color() formats. Automatically skips preprocessor variables and CSS custom properties.
